services:
  - type: web
    name: bird-migration-map
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: EBIRD_API_KEY
        sync: false